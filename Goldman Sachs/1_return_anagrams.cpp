/* Question
Given an array of strings, return all groups of strings that are anagrams.
*/

#include <bits/stdc++.h>
using namespace std;

void printAnagram(char* arr[], int n)
{

}

int main ()
{
    char* arr={"cat", "dog", "tac", "god", "act", "gdo"};
    int n=sizeof(arr)/sizeof(arr[0]);
    printAnagram(arr,n);
    return 0;
}